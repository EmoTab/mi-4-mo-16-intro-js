// variable declaration
var tomatoPrice = 25;

// variable value update
tomatoPrice = 30;

// add something with the previous value
var price1 = 31;
var price2 = 10;

// set a new vlue
// price1 = 35;

// add some value with the existing value
price1 = price1 + 10;
// console.log(price1);

// adding same variable shorthand
// += means adding some value with existing value
price1 += 10;

price2 = price2 - 5;

// shorthand
// -= means deducting some values from the current value of the variable
price2 -= 5;

// *=
price1 *= 5;

// /=
price /= 5;

var age = 15;
age = age + 1;
// shorthand ++ means incrementing 1 with existing value
age ++;


age = age -1;
// shorthand -- means decrementing 1 with existing value
age --;

